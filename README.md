# stock-picker-app

Static personal dashboard served via GitHub Pages. All data in this repo is
encrypted at rest (`data.enc`, AES-256-GCM); the page decrypts locally with a
passcode. Updated automatically by a workflow in a private repo.
